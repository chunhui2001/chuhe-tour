import { CellInputCrossMoveDirective } from './cell-input-cross-move.directive';

describe('CellInputCrossMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new CellInputCrossMoveDirective();
    expect(directive).toBeTruthy();
  });
});
