import { SelectTextOnFocusDirective } from './select-text-on-focus.directive';

describe('SelectTextOnFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectTextOnFocusDirective();
    expect(directive).toBeTruthy();
  });
});
